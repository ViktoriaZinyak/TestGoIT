import { ProfileList } from '../ProfileList/ProfileList';
import { Container } from './App.styled';

export const App = () => {
  return (
    <Container>
      <ProfileList />
    </Container>
  );
};
